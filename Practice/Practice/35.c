/*
#include <stdio.h>

typedef struct stuent {
	char perA[5];
	char perB[10][3];
} student[4];

int main() {

	char name[10];
	student student;

	for (int i = 0; i < 4; i++) {
		scanf("%s %s %s %s", &student[i].perA, &student[i].perB, &student[i].perB, &student[i].perB);
	}

	

	printf("이름을 입력하세요 : ");
	scanf("%s", &name);
}

int ABC(student stu[], char* perA, char* perB) {
	
}
*/