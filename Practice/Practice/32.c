/*
#include <stdio.h>
int average(student);

struct student {
	int num;
	int kor;
	int eng;
	int math;
} studentinfo;

int main() {

	printf("학번과 국영수 점수를 입력하세요");
	scanf("%d %d %d %d", &studentinfo.num, &studentinfo.kor, &studentinfo.eng, &studentinfo.math);

	printf("평균 점수 : %d", average(studentinfo));

}

int average(stuent) {
	int ave;
	ave = (studentinfo.kor + studentinfo.eng + studentinfo.math) / 3;
	return ave;
}
*/